class LineChart {
    constructor(_config, _data) {
        this.config = {
            parentElement: _config.parentElement,
        }

        this.data = _data;

        this.initVis();
    }

    initVis() {
        let vis = this;

        let containerWidth = 0; // placeholders - need to fill in
        let containerHeight = 0;

    }

    updateVis() {

    }

    renderVis() {

    }
}